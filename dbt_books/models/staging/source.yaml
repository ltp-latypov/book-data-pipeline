version: 2

sources:
  - name: books_cleaned
    # Use your NEW project ID here
    database: "{{ target.project }}" 
    schema: books_cleaned
    tables:
      - name: books
      - name: ratings
      - name: users