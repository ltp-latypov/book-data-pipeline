version: 2

sources:
  - name: books_cleaned
    # Use your NEW project ID here
    database: "{{ env_var('GCP_PROJECT_ID') }}" 
    schema: books_cleaned
    tables:
      - name: books
      - name: ratings
      - name: users